# Cleanup

This repo does not provision cloud resources.

Remove generated local outputs when you are done:

```bash
rm -r artifacts
rm -r reports
```

On Windows, delete those folders manually or with PowerShell if you created them locally.
